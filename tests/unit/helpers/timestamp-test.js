import { timestamp } from 'answer-board/helpers/timestamp';
import { module, test } from 'qunit';

module('Unit | Helper | timestamp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timestamp([42]);
  assert.ok(result);
});
